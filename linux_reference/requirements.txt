# Gaze Mouse System — Python dependencies
# Install:  pip install -r requirements.txt --break-system-packages
# OR:       source .venv/bin/activate && pip install -r requirements.txt

# Socket.IO server (backend) + client (calibration.py)
python-socketio[client]
eventlet

# OS mouse control
pyautogui

# Screen resolution detection
screeninfo

# websocket transport for socketio client
websocket-client

# Head pose compensation (FIX-7)
mediapipe>=0.10.0
opencv-python-headless
numpy

# Note: the following require SYSTEM packages, not pip:
#
#   pyatspi  →  sudo apt install python3-pyatspi
#   GTK      →  sudo apt install python3-gi python3-gi-cairo
#                               gir1.2-gtk-3.0 gir1.2-gdk-3.0
#   Xvfb     →  sudo apt install xvfb
#
# Full system dependency install:
#   sudo apt install xvfb chromium-browser python3-pyatspi \
#                    python3-gi python3-gi-cairo gir1.2-gtk-3.0 \
#                    gir1.2-gdk-3.0 xdotool
